<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sort">

    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Zasady"
            android:label="@string/title_activity_zasady"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".zasady_sortowania_odpadow" />
        <activity android:name=".Rejestracja" />
        <activity android:name=".Logowanie" />
        <activity android:name=".brazowy" />
        <activity android:name=".czarny" />
        <activity android:name=".zielony" />
        <activity android:name=".zolty" />
        <activity android:name=".niebieski" />
        <activity android:name=".Skanowanie_dodawanie" />
        <activity android:name=".DodawanieKoduDoBazyActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>